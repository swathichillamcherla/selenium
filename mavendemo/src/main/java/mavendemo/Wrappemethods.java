package mavendemo;

import org.openqa.selenium.WebDriver;

public class Wrappemethods {

	public class Wrappermethods {
		WebDriver driver;
		
		public void Insertapp(String url)

		{
			System.out.println("webdriver.chrome.driver", "C:\\Users\\chkis\\Desktop\\selenimSsoft\\chromedriver_win32\\chromedriver.exe");
			driver.manage().window().maximize();
			driver.manage().timeouts().implicitlyWait(30,TimeUit.seconds);
			driver.get(url);
			
			public void enterbyid (string.val.sendkeys(name));
		}
		public void clickxpath(sting vall) {
			driver.close();
			{
			
		}
	}

}
